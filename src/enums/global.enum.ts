import { ConfigService } from '@nestjs/config';

// Environment MAP
export enum MicroserviceTokenList {
  userMicroService = 'USER_MICROSERVICE_TOKEN'
}

//microservices name map
export enum MicroservicesList {
  userMicroService = 'userMicroService'
}
